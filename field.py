# Field upon which invaders and tank clash.
from weapon import *

# Default parameters, change as desired
DEFAULT_FIELD_WIDTH = 50
DEFAULT_FIELD_HEIGHT = 75

class Field:

  def __init__(self, width = DEFAULT_FIELD_WIDTH, height = DEFAULT_FIELD_HEIGHT):
    self.width = width
    self.height = height
  
  def spawnInvader():
    # TODO
    return
  
  def spawnTank():
    # TODO
    return